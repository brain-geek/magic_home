use Mix.Config

# Set up wired connection with DHCP
# See more examples at https://github.com/nerves-project/nerves_network

config :nerves_network, :default,
  eth0: [
    ipv4_address_method: :dhcp
  ]
